# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
gunicorn==26.0.0

# --- Database ---
sqlalchemy==2.0.51
alembic==1.19.0
asyncpg==0.31.0          # async Postgres driver (runtime)
psycopg[binary]==3.3.4   # sync Postgres driver (used by Alembic migrations)

# --- Settings / validation ---
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0

# --- Auth / security ---
pyjwt==2.13.0
bcrypt==5.0.0

# --- File upload support (admin property/image forms) ---
python-multipart==0.0.32

# --- Calendar sync (Airbnb / VRBO iCal feeds) ---
icalendar==7.2.2
httpx==0.28.1

# --- Scheduling (calendar re-sync + housekeeping jobs) ---
apscheduler==3.11.3

# --- Rate limiting ---
slowapi==0.1.10

# --- Dev / test ---
pytest==9.1.1
pytest-asyncio==1.4.0
